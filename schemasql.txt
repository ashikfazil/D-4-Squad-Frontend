
USE project;

CREATE TABLE transactions (
    t_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    category VARCHAR(100) NOT NULL,
    transaction_type ENUM('buy', 'sell') NOT NULL,
    price DOUBLE NOT NULL,
    date DATETIME NOT NULL,
    quantity INT NOT NULL
);
INSERT INTO transactions (name, category, transaction_type, price, date, quantity)
VALUES ('Apple Inc.', 'Stock', 'buy', 189.75, '2025-08-02 12:00:00', 10);
ALTER TABLE transactions DROP COLUMN shortForm;
truncate table transactions;
describe assets;

create table wallet(id int auto_increment primary key, 
balance double not null default 10000.0);
insert into wallet(balance) values (10000.0);
ALTER TABLE wallet
ADD COLUMN user_email VARCHAR(255) DEFAULT NULL,
ADD COLUMN portfolio_alert_threshold DECIMAL(15, 2) DEFAULT NULL,
ADD COLUMN alert_sent BOOLEAN DEFAULT FALSE;
drop table assets;
CREATE TABLE assets (
    Asset_id INT AUTO_INCREMENT PRIMARY KEY,
    Name VARCHAR(100) NOT NULL,
    shortForm VARCHAR(20) NOT NULL,
    price DOUBLE NOT NULL,
    volume DOUBLE NOT NULL,
    category VARCHAR(50) NOT NULL,
    createdAt DATE NOT NULL
);
select * from assets;

